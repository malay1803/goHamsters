<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%=title%></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
    <script src="https://code.jquery.com/jquery-3.3.1.js"></script>
</head>
<body>
     <div class="d-flex justify-content-center align-items-center">
        <div class="p-5 shadow-lg bg-body" style="width:max-content;">
            <form action="/resetPassword/<%=token%>" method="POST" onsubmit="checkPass()" class="sign-in-form" id="forget" >
                <h2>Reset your Password</h2>
                <h5 class="mt-5 mb-3">for <%=email%></h5>
                <input type="hidden" name="token" id="token" value="<%=token%>">
                New password
                    <input type="password" class="password" id="newPass" pattern="^(?=.*[A-Za-z])(?=.*\d)[A-Za-z\d]{8,}$" title="Min 8 characters, 1 letter and 1 number." name="password"/> <br>
                <div class="d-flex justify-content-center mt-4">
                    <button type="submit" class="p-2 px-3 btn btn-outline-success w-100">Save</button>
                </div>
            </form>
        </div>
    </div>

</body>

<script>
    function checkPass(){
        // $(".errorMessage").remove()
        // if(password===""){
        //   $("#newPass").append(`<div class="errorMessage">Password can't be empty.</div>`)
        // }
        // else if(!(/^(?=.*[A-Za-z])(?=.*\d)[A-Za-z\d]{8,}$/.test(password))){
        //   $("#newPass").append(`<div class="errorMessage">Min 8 characters, 1 letter and 1 number.</div>`)
        // }else{
        //   count+=1
        // }
        // if(count===1){
        //     return true
        // }else{
        //     return false
        // }
    }
</script>
</html>